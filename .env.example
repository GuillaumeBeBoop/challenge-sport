# Le code d'accès partagé entre les deux joueurs. Seule variable obligatoire.
ACCESS_CODE=change-moi

# Décommenter si le site tourne en HTTP simple (réseau local, test) : sans ça
# le navigateur refuse le cookie de session et la connexion échoue en boucle.
# COOKIE_SECURE=false
